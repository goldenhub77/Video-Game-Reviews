<h2><%= @title %> <%= @review_for_form.title %></h2>

<%= render "reviews/review_form" %>
<%= button_to "Delete", review_path(@review_for_form), data: { confirm: "Are you sure?" }, class: "col-md-4 col-md-offset-4 btn btn-danger", method: :delete %>
